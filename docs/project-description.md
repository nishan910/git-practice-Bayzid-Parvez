## Project Overview
A practical Git & GitHub workflow assignment.

### Features:
- Organized structure (`src`, `docs`, `tests`).
- Configured `.gitignore`.
- Python logic: `add`, `subtract`, and `multiply`.
- Workflow: Branching, merging, and atomic commits.